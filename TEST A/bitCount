int bitCount(int x)
{
/*int bitCount(int x) {
    基准情况：如果 x 为 0，那么返回值是 0
    if (x == 0) {
        return 0;
    }
     递归调用，清除最低有效位上的1，并加上1
    return 1 + bitCount(x & (x - 1));
}*/
//上面就是常规递归对于这个bitCount的实现，核心目的就是要实现if
int f = ~(!x) +1
return (0 & f)|((~f)&(1 + bitCount(x & (x + (~0)))));
}
    


